import { Header } from '@/components/header/Header'
import Image from 'next/image'

const itemList = ["Cursos disponibles", "Mis cursos", "Materiales", "Foro", "Certificados"]

export default function Home() {
    return (
        <div className='flex-1'>Bienvenido</div>
    )

}
